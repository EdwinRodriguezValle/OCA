package Book.ChapterI;


public class Zoo {
    /**
     * Para compilar desde la línea de comando necesitas tienes que primero ejecutar el comando 'javac + el nombre de la clase"
     * este genera un archivo de bytes y para imprimir o ejecutar hace java + nombre de archivo y si tiene argumentos le pasas la cantidad
     * de argumentos.
     * @param args
     */
    public static void main(String[] args) {
        System.out.println(args[0]);
        System.out.println(args[1]);
    }
}
